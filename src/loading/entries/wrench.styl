@import '../../vars.styl'
@import '../../basic.styl'

+keyframes(ld-wrench)
  20%, 36%, 70%, 86%
    transform(rotate(0deg))
  0%, 50%, 100%
    transform(rotate(45deg))

.ld.ld-wrench
  animation(ld-wrench 1s infinite)
